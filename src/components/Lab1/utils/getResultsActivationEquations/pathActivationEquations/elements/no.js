//@flow
import type { ActivationEquationFunc } from "../../types";

export const getActivationEquation: ActivationEquationFunc = (inputCount, activatioPathSymbol, activatioPathNumber) => {
  const ret = [];
  ret.push(activatioPathSymbol);
  return ret;
};
